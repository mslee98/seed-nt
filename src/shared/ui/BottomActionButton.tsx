/**
 * BottomCTA·BottomSheetFooter 등 화면 하단 고정 CTA용 ActionButton.
 * 내부적으로 `MotionActionButton`(tap scale 0.98)을 사용합니다.
 */
export { MotionActionButton as BottomActionButton } from '../motion/MotionActionButton'
